@page "/order/create"

@inject IHttpClientFactory HttpClientFactory
@inject HttpClient HttpClient
@inject NavigationManager NavigationManager
@inject IJSRuntime JSRuntime
@attribute [Authorize(Roles = "Admin")]

<h3 class="text-center mt-4 text-primary">Create New Product</h3>

<div class="card shadow-lg p-4">
    <div class="row">
        <div class="col-md-6">
            <SfTextBox @bind-Value=order.OrderNumber FloatLabelType="Syncfusion.Blazor.Inputs.FloatLabelType.Auto" Placeholder="Enter Order Number" />
        </div>
        <div class="col-md-6">
            <SfDatePicker @bind-Value=order.Date FloatLabelType="Syncfusion.Blazor.Inputs.FloatLabelType.Auto" Placeholder="Enter Order Date" />
        </div>
    </div>
    <div class="row mt-3">
        <div class="col-md-6">
            <SfComboBox TValue="int" TItem="ProductListDto" @bind-Value="order.ProductId" DataSource="@products" FloatLabelType="Syncfusion.Blazor.Inputs.FloatLabelType.Auto" Placeholder="Select Product">
                <ComboBoxFieldSettings Text="Description" Value="Id"></ComboBoxFieldSettings>
            </SfComboBox>

          @*   <SfMultiColumnComboBox TValue="int" TItem="ProductListDto" @bind-Value="order.ProductId" DataSource="products">
                <MultiColumnComboboxColumns>
                    <MultiColumnComboboxColumn Field="ProductListDto.Code" HeaderText="Code" Width="120"></MultiColumnComboboxColumn>
                    <MultiColumnComboboxColumn Field="ProductListDto.Descrtiption" HeaderText="Description" Width="120"></MultiColumnComboboxColumn>
                </MultiColumnComboboxColumns>
            </SfMultiColumnComboBox> *@
        </div>
        <div class="col-md-6">
            <SfNumericTextBox @bind-Value=order.Quantity FloatLabelType="Syncfusion.Blazor.Inputs.FloatLabelType.Auto" Placeholder="Enter Quantity" />
        </div>
    </div>
</div>
<h3 class="text-center mt-4 text-primary">Materials</h3>

<div class="card shadow-lg p-4">
    <SfButton CssClass="e-success" IconCss="e-icons e-plus" @onclick="AddMaterial"> Add Material </SfButton>
    @foreach (var trLine in order.StockTransaction.StockTransactions.Where(st => st.TransactionType == TransactionType.Input))
    {
        <div class="row">
            <div class="col-md-6">
                <SfDropDownList TValue="int" TItem="ProductListDto" @bind-Value="trLine.ProductId" DataSource="@products" FloatLabelType="Syncfusion.Blazor.Inputs.FloatLabelType.Auto" Placeholder="Select Product">
                    <DropDownListFieldSettings Text="Description" Value="Id"></DropDownListFieldSettings>
                </SfDropDownList>
            </div>
            <div class="col-md-3">
                <SfNumericTextBox @bind-Value=trLine.Quantity FloatLabelType="Syncfusion.Blazor.Inputs.FloatLabelType.Auto" Placeholder="Enter Quantity" />
            </div>
            <div class="col-md-3">
                <SfNumericTextBox @bind-Value=trLine.Price FloatLabelType="Syncfusion.Blazor.Inputs.FloatLabelType.Auto" Placeholder="Enter Cost" />
            </div>
        </div>
    }

</div>

<div class="d-flex justify-content-center mt-4">
    <SfButton CssClass="e-primary" @onclick="Save">Save</SfButton>
</div>
@code {
    private ProductionOrderCreateDto order = new();
    private List<ProductListDto> products = new();

    protected override async Task OnInitializedAsync()
    {
        HttpClient = HttpClientFactory.CreateClient("Auth");
        products = (await HttpClient.GetFromJsonAsync<List<ProductListDto>>("products")) ?? [];
    }
    private void AddMaterial(MouseEventArgs args)
    {
        var newLine = new StockTransactionLineDto();
        newLine.TransactionType = TransactionType.Input;
        order.StockTransaction.StockTransactions.Add(new StockTransactionLineDto());
    }
    private async Task Save(MouseEventArgs args)
    {
        var outputLine = new StockTransactionLineDto();
        outputLine.TransactionType = TransactionType.Output;
        outputLine.ProductId = order.ProductId;
        outputLine.Quantity = order.Quantity;
        order.StockTransaction.StockTransactions.Add(outputLine);
        var result = await HttpClient.PostAsJsonAsync("orders", order);
        if (result.IsSuccessStatusCode)
        {
            NavigationManager.NavigateTo("orders");
        }
        else
        {
            await JSRuntime.InvokeVoidAsync("alert", "Error creating order");
        }

    }
}